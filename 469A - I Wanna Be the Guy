#include <iostream>
#include <set>
using namespace std;

int main()
{
    int n;
    cin>>n;
    set<int> levels;
    int p;
    cin>>p;
    for(int i=0;i<p;i++){
        int l;
        cin>>l;
        levels.insert(l);
    }
    int q;
    cin>>q;
    for(int i=0;i<q;i++){
        int l;
        cin>>l;
        levels.insert(l);
    }
    if(levels.size()==n){
        cout<<"I become the guy.";
    }
    else{
        cout<<"Oh, my keyboard!";
    }
    return 0;
}
