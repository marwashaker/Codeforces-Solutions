#include <iostream>
#include <string>
#include <algorithm>
#include <cctype>
using namespace std;

int main()
{
    int n;
    cin>>n;
    string s;
    cin>>s;
   for(int i=0;i<n;i++){
    s[i]=tolower(s[i]);
   }
    sort(s.begin(), s.end());
   int coun=0;
      for(int i=0;i<n;i++){
      if(i==0||s[i]!=s[i-1]){
        coun++;
        }
      }
   if(coun==26){
    cout<<"YES";
   }
   else{
    cout<<"NO";
   }
    return 0;
}
